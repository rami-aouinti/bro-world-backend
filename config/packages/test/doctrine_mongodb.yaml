doctrine_mongodb:
    default_database: '%env(default::string:MONGODB_DB)%_test'
    connections:
        default:
            server: '%env(default::string:MONGODB_URI)%'
            options: { }
    document_managers:
        default:
            auto_mapping: true
